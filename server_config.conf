const port = 8080
const max_connections = 100
const timeout = 30.5
const backends = (1, 2, 3)
const ssl = table([
    enabled = 1,
    certificate = 1
])
const thread_pool_size = |max_connections - 300|